import { useTheme } from '@mui/material/styles';
import { Typography } from '@mui/material';

const MediSearchLogo = () => {
    const theme = useTheme();

    return (
        <>
            <Typography variant="h3">MediSearch</Typography>
        </>
    );
};

export default MediSearchLogo;
